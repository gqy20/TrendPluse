# 📄 PRD：AnthroPulse（PRD v1.0）

## 1. 背景与问题

Anthropic 团队在 GitHub 上发布的 SDK、工具链、Agent workflow 及评测体系变化，往往蕴含重要趋势信号：

* SDK 抽象如何演化 → 影响开发范式
* Tooling/CLI 的新能力 → 影响工程体系
* Agent workflow / eval 方法变化 → 影响研究方向
* 安全与评测体系的更新 → 影响行业标准

但这些变化分散在多个仓库、多个 PR/commit/release 中，且更新频繁，人工跟踪成本高且容易遗漏。
因此需要一个自动化系统：**定期收集 → 语义提炼 → 长期归档 → 形成可检索的趋势情报站点**。

---

## 2. 产品目标

### 2.1 核心目标（Must）

* 每天自动追踪 Anthropic 核心 GitHub 仓库更新（SDK & tooling + agent workflow / eval）
* 自动识别“新功能 / 新思路 / 趋势信号”
* 生成结构化日报，并在 GitHub Pages 上持续发布与归档
* 支持按日期、按 repo、按信号类型浏览历史

### 2.2 次要目标（Should）

* 生成周报：对趋势连续性进行归因与总结
* 形成“趋势信号库”：持续积累可检索
* 输出结构化 JSON 数据，方便二次分析/订阅

### 2.3 未来目标（Could）

* 趋势图表（过去 7/30 天信号强度/主题）
* RSS / 邮件订阅 / Slack 推送
* 自动发现新仓库（自动维护 repo 列表）
* 多组织扩展（如 openai、google-deepmind 等）

---

## 3. 用户画像与使用场景

### 3.1 目标用户

1. **工程负责人/SDK 开发者**：关注 SDK API、工具链演化、最佳实践
2. **研究者/Agent 开发者**：关注 agent workflow、eval 体系、benchmarks、新范式
3. **产品/战略分析者**：关注新方向信号、趋势变化、生态扩张情况

### 3.2 核心场景

* 每天 3 分钟浏览：今天 Anthropic 工程/研究上发生了什么？
* 每周复盘：哪些方向持续增强？哪些是一次性变化？
* 深入追踪：某个 repo（如 claude-code 或 eval 框架）有哪些关键演进？

---

## 4. 范围（Scope）

### 4.1 v1.0（MVP）范围

#### 数据覆盖（Input）

* Repositories：人工维护的核心仓库列表（repos.yaml）
* Events（过去 24h / 48h）：

  * merged PR（重点）
  * releases（近 7 天）
  * commits（作为补充，避免漏掉直接 push）
  * issues/discussions（可选：仅限 enhancement/proposal 标签）

#### 输出（Output）

* `Daily Brief`：每日总览（趋势主题 + 重点信号）
* `Signals`：按工程/研究两条线输出信号列表
* `Repo Timeline`：每个仓库的关键演进 timeline（只保留重要事件）
* `Raw Data`：JSON 快照与结构化数据

#### 站点（Docs Style）

* MkDocs Material
* GitHub Pages 部署
* 搜索支持
* 每日归档

---

### 4.2 v1.1（增强）范围

* 周报（每周自动生成）
* 信号去重/归因（同一主题跨天归并）
* 基础趋势统计（7d 信号数、impact 分布）

---

## 5. 核心功能需求（Functional Requirements）

### 5.1 数据采集（GitHub）

* 支持配置仓库列表（org + repo）
* 拉取 PR/commit/release 的必要字段
* 增量更新：避免重复抓取历史
* 对外部 API 失败具有重试与降级机制

**验收标准**

* 每天可稳定产出当日数据快照
* 抓取结果可复现（保存原始数据）

---

### 5.2 重要事件筛选（Candidate Selection）

系统从当日更新中筛选候选事件（减少噪音）：

* merged PR > open PR
* label 包含：feature/enhancement/eval/tooling/agent/workflow/safety
* PR 描述中包含关键词：introduce/add/support/implement/eval/benchmark/tool/schema
* Releases 默认全部纳入

**验收标准**

* 输出候选事件数控制在可读范围（建议 5–30）
* 不漏掉 release 或重大 breaking change

---

### 5.3 Claude Agent 分析（Signals Extraction）

Claude 需要输出结构化结果，包含：

**工程趋势信号（Engineering Signals）**

* SDK API 新能力 / 新抽象 / 新工具链
* 可能影响开发范式的变化

**研究趋势信号（Research Signals）**

* agent workflow、tool-use、planning、routing
* eval/benchmark 框架变化
* 可能影响未来模型/agent 开发趋势

每条信号应包含：

* title
* type（capability/abstraction/workflow/eval/safety/performance）
* impact score（1–5）
* why_it_matters（1–2 句）
* sources（PR/commit/release 链接）
* followups（可选）

**验收标准**

* 输出必须稳定可解析（JSON + Markdown）
* 引用必须可追溯（sources 必须来自输入）
* 不允许编造不存在的数据

---

### 5.4 站点生成与发布

* 自动生成 MkDocs docs 目录结构：

  * `docs/daily/YYYY-MM-DD.md`
  * `docs/signals/engineering/YYYY-MM.md`
  * `docs/signals/research/YYYY-MM.md`
  * `docs/repos/<repo>.md`
* 自动生成导航（mkdocs.yml 或用 `mkdocs.yml + awesome-pages plugin`）
* GitHub Pages 自动部署

**验收标准**

* 每天站点更新可访问
* 页面结构清晰，支持搜索
* 任何历史页面可回溯

---

## 6. 非功能需求（Non-functional Requirements）

### 6.1 可维护性

* pipeline 模块化（fetch / preprocess / analyze / build_docs）
* 输出 schema 明确版本号
* 关键配置可调（repo list / labels / keyword patterns）

### 6.2 成本与稳定性

* Token 控制：diff 摘要抽样，避免全文喂给 Claude
* API rate limit 控制：GraphQL 批量查询优先
* 出错降级：如果 Claude 失败，至少生成 raw report + 抓取数据

### 6.3 安全

* API key 使用 GitHub Secrets
* 输出内容不包含敏感信息（仅公开仓库数据）
* 生成内容必须可追溯引用来源链接

---

## 7. 数据与输出格式

### 7.1 Raw snapshot（每天）

`data/snapshots/2026-01-02/<repo>.json`

### 7.2 Processed metrics（每天）

`data/processed/2026-01-02.json`

### 7.3 Claude outputs

* `reports/daily/2026-01-02.md`
* `reports/daily/2026-01-02.json`

---

## 8. 技术方案概述

### 8.1 技术栈

* Python 3.11+
* GitHub REST + GraphQL API
* Claude Agent Python SDK / Anthropic SDK
* MkDocs Material
* GitHub Actions + GitHub Pages

### 8.2 Pipeline

1. Fetcher：拉取 events
2. Preprocess：抽取候选 + 限制 token
3. Analyzer（Claude）：生成信号与趋势
4. Docs Builder：生成 docs
5. Deploy：mkdocs build & publish

---

## 9. 里程碑（Milestones）

### M1：基础跑通（1–2 天）

* 配置仓库列表
* fetch → raw snapshots
* MkDocs 站点能正常发布

### M2：信号提炼（3–5 天）

* Claude agent 输出稳定 JSON + daily markdown
* daily brief + signals 页面生成

### M3：可用版本（1 周）

* repo timeline 页面自动生成
* 站点归档与搜索体验稳定
* 错误降级与日志完善

### M4：趋势增强（2–3 周）

* weekly digest
* signals 聚类与去重
* 简单趋势图表

---

## 10. 风险与应对（Risks）

| 风险         | 描述                      | 应对                               |
| ---------- | ----------------------- | -------------------------------- |
| 噪音过多       | issues/discussions 质量不一 | 先仅 merged PR + release，逐步引入      |
| Token 爆    | diff 过大                 | diff 抽样 + 分段 summarize           |
| API 限速     | GitHub Rate limit       | GraphQL 批量 + cache + incremental |
| 输出漂移       | Claude 输出结构不稳定          | 强制 JSON schema + 失败回退            |
| Repo 列表不稳定 | 新 repo 出现/迁移            | 周期性自动发现（v1.1+）                   |

---

## 11. 成功指标（Success Metrics）

* 每天自动生成日报成功率 ≥ 95%
* 每天“重点信号”数量在 5–15 条之间（可读）
* 每条信号包含可追溯来源（100%）
* 站点历史可检索（search 命中率高）
* 每周输出趋势总结具备连续性（v1.1）

---

# ✅ 总结：你这个项目的定位（一句话）

**AnthroPulse** 是一个面向工程与研究的开源情报站：每天自动追踪 Anthropic GitHub 核心仓库更新，并用 Claude 提炼“工程趋势信号”和“研究趋势信号”，持续归档于可检索的 MkDocs 站点。
